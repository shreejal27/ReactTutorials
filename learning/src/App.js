

function App() {
  const colors =  ["Red", "Orange", "Yellow", "Green", "Blue", "White"]
  return (
    <div>
      {/* const colorElements = colors.map(cname => {
        return {cname}
      }) */}
      return(
        <div>
          {colors}
        </div>
      )
    </div>
  );
}

export default App;
